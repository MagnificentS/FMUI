<UserControl x:Class="FMUI.Wpf.UI.Cards.TransferBudgetCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="320"
             d:DesignHeight="180"
             Background="Transparent">
    <Border Background="{StaticResource PanelBackgroundBrush}"
            BorderBrush="{StaticResource PanelBorderBrush}"
            BorderThickness="1"
            CornerRadius="6"
            Padding="16">
        <StackPanel Orientation="Vertical" Spacing="12">
            <StackPanel Orientation="Vertical" Spacing="4">
                <TextBlock Text="Transfer Budget"
                           FontSize="16"
                           Foreground="{StaticResource PrimaryTextBrush}"/>
                <TextBlock x:Name="TransferBudgetValue"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource AccentPrimaryBrush}"/>
            </StackPanel>
            <StackPanel Orientation="Vertical" Spacing="4">
                <TextBlock Text="Wage Budget"
                           FontSize="16"
                           Foreground="{StaticResource PrimaryTextBrush}"/>
                <TextBlock x:Name="WageBudgetValue"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource PrimaryTextBrush}"/>
            </StackPanel>
            <StackPanel Orientation="Vertical" Spacing="4">
                <TextBlock Text="Available Wages"
                           FontSize="14"
                           Foreground="{StaticResource NeutralTextBrush}"/>
                <ProgressBar x:Name="WageUsageBar"
                             Height="12"
                             Minimum="0"
                             Maximum="100"
                             Foreground="{StaticResource AccentPrimaryBrush}"/>
                <TextBlock x:Name="WageUsageLabel"
                           FontSize="12"
                           Foreground="{StaticResource NeutralTextBrush}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
