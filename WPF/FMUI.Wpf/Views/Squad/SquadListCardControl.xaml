<UserControl x:Class="FMUI.Wpf.Views.Squad.SquadListCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:FMUI.Wpf.Models"
             xmlns:views="clr-namespace:FMUI.Wpf.Views"
             d:DesignHeight="400"
             d:DesignWidth="300"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">
    <UserControl.Resources>
        <DataTemplate x:Key="DescriptorListItemTemplate" DataType="{x:Type models:CardListItem}">
            <Border Margin="0,0,0,10"
                    Padding="0,0,0,8"
                    Background="Transparent"
                    BorderBrush="#1F2A39"
                    BorderThickness="0 0 0 1"
                    ToolTipService.InitialShowDelay="250"
                    ToolTipService.ShowDuration="8000">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="Transparent" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#162434" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Border.ToolTip>
                    <ToolTip Style="{StaticResource CardToolTipStyle}">
                        <StackPanel>
                            <TextBlock Text="{Binding Primary}" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Secondary}"
                                       Margin="0,6,0,0"
                                       Foreground="{StaticResource NeutralTextBrush}"
                                       Visibility="{Binding Secondary, Converter={StaticResource NullToVisibilityConverter}}" />
                            <TextBlock Text="{Binding Tertiary}"
                                       Margin="0,4,0,0"
                                       Foreground="{StaticResource NeutralTextBrush}"
                                       Visibility="{Binding Tertiary, Converter={StaticResource NullToVisibilityConverter}}" />
                        </StackPanel>
                    </ToolTip>
                </Border.ToolTip>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{Binding Primary}" Style="{StaticResource CardListPrimaryTextStyle}" />

                    <TextBlock Grid.Column="1"
                               Text="{Binding Secondary}"
                               Style="{StaticResource CardListSecondaryTextStyle}"
                               TextAlignment="Right"
                               Visibility="{Binding Secondary, Converter={StaticResource NullToVisibilityConverter}}" />

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Border Background="#2EC4B622"
                                Padding="8,2"
                                CornerRadius="10"
                                Margin="0,0,8,0"
                                Visibility="{Binding Accent, Converter={StaticResource NullToVisibilityConverter}}">
                            <TextBlock Text="{Binding Accent}"
                                       Foreground="{StaticResource AccentPrimaryBrush}"
                                       FontSize="11"
                                       FontWeight="SemiBold" />
                        </Border>
                        <TextBlock Text="{Binding Tertiary}"
                                   Style="{StaticResource CardListSecondaryTextStyle}"
                                   Visibility="{Binding Tertiary, Converter={StaticResource NullToVisibilityConverter}}" />
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <ItemsControl ItemsSource="{Binding Items}"
                  ItemTemplate="{StaticResource DescriptorListItemTemplate}" />
</UserControl>
